{
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/app(.*)", "dest": "/dist/index.html" },
    { "src": "/(.*)", "dest": "/public/$1" }
  ]
}
