{
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/app(.*)", "dest": "/index.html" },
    { "src": "^/$", "dest": "/landing.html" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
